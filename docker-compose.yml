# ── Shared worker template ────────────────────────────────────
x-worker: &worker
  profiles: [workers]
  build:
    context: ./worker
    dockerfile: Dockerfile
  restart: unless-stopped
  networks:
    - llmnet

x-worker-env: &worker-env
  WORKER_PORT: "8001"
  COORDINATOR_URL: http://control:8000
  LLM_MODEL: ${LLM_MODEL:-llama3.1:8b}
  OLLAMA_MAX_OUTPUT_TOKENS: ${OLLAMA_MAX_OUTPUT_TOKENS:-300}
  OLLAMA_KEEP_ALIVE: ${OLLAMA_KEEP_ALIVE:-30m}
  OLLAMA_SSL_VERIFY: "false"
  HEARTBEAT_INTERVAL_SECONDS: "0.5"
  METRICS_INTERVAL_SECONDS: "1"
  REGISTRATION_RETRY_SECONDS: "2"
  LATENCY_WINDOW: "20"
  MAX_TOKENS_PER_SECOND: "80"
  OLLAMA_MAX_CONCURRENT_REQUESTS: ${OLLAMA_MAX_CONCURRENT_REQUESTS:-4}

services:

  # ── Master ────────────────────────────────────────────────────
  chroma:
    profiles: [master]
    image: chromadb/chroma:0.5.23
    ports:
      - "8002:8000"
    volumes:
      - chroma-data:/chroma/chroma
    environment:
      IS_PERSISTENT: "TRUE"
      ANONYMIZED_TELEMETRY: "FALSE"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    networks:
      - llmnet

  control:
    profiles: [master]
    build:
      context: ./control
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "8000:8000"
    volumes:
      - chroma-onnx-cache:/root/.cache/chroma
    env_file: .env
    environment:
      HEARTBEAT_INTERVAL_SECONDS: ${HEARTBEAT_INTERVAL_SECONDS:-0.5}
      WORKER_MISSED_HEARTBEATS: ${WORKER_MISSED_HEARTBEATS:-6}
      NGINX_PROXY_READ_TIMEOUT: ${NGINX_PROXY_READ_TIMEOUT:-86400}
    depends_on:
      chroma:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 5m
    restart: unless-stopped
    networks:
      - llmnet

  prometheus:
    profiles: [master]
    image: prom/prometheus:v2.54.1
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/usr/share/prometheus/console_libraries"
      - "--web.console.templates=/usr/share/prometheus/consoles"
    depends_on:
      control:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - llmnet

  grafana:
    profiles: [master]
    image: grafana/grafana:11.3.1
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus
    restart: unless-stopped
    networks:
      - llmnet

  # ── Workers (Mac containers → Thunder Compute Ollama) ─────────
  worker-1:
    <<: *worker
    ports:
      - "9001:8001"
    environment:
      <<: *worker-env
      WORKER_NAME: worker-1
      WORKER_HOST: worker-1
      OLLAMA_BASE_URL: ${THUNDER_COMPUTE_URL_1:-http://host.docker.internal:11434}
      THUNDER_METRICS_URL: ${THUNDER_METRICS_URL_1:-}

  worker-2:
    <<: *worker
    ports:
      - "9002:8001"
    environment:
      <<: *worker-env
      WORKER_NAME: worker-2
      WORKER_HOST: worker-2
      OLLAMA_BASE_URL: ${THUNDER_COMPUTE_URL_2:-http://host.docker.internal:11434}
      THUNDER_METRICS_URL: ${THUNDER_METRICS_URL_2:-}

  worker-3:
    <<: *worker
    ports:
      - "9003:8001"
    environment:
      <<: *worker-env
      WORKER_NAME: worker-3
      WORKER_HOST: worker-3
      OLLAMA_BASE_URL: ${THUNDER_COMPUTE_URL_3:-http://host.docker.internal:11434}
      THUNDER_METRICS_URL: ${THUNDER_METRICS_URL_3:-}

  worker-4:
    <<: *worker
    ports:
      - "9004:8001"
    environment:
      <<: *worker-env
      WORKER_NAME: worker-4
      WORKER_HOST: worker-4
      OLLAMA_BASE_URL: ${THUNDER_COMPUTE_URL_4:-http://host.docker.internal:11434}
      THUNDER_METRICS_URL: ${THUNDER_METRICS_URL_4:-}

  worker-5:
    <<: *worker
    ports:
      - "9005:8001"
    environment:
      <<: *worker-env
      WORKER_NAME: worker-5
      WORKER_HOST: worker-5
      OLLAMA_BASE_URL: ${THUNDER_COMPUTE_URL_5:-http://host.docker.internal:11434}
      THUNDER_METRICS_URL: ${THUNDER_METRICS_URL_5:-}

  # ── Client ────────────────────────────────────────────────────
  client:
    profiles: [client]
    build:
      context: .
      dockerfile: client/Dockerfile
    environment:
      COORDINATOR_URL: http://control:80
    depends_on:
      control:
        condition: service_healthy
    restart: "no"
    networks:
      - llmnet

networks:
  llmnet:
    driver: bridge

volumes:
  chroma-data:
  chroma-onnx-cache:
  grafana-data:
